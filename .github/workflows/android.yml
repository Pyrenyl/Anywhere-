name: Build Release

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    if: github.repository_owner == 'Pyrenyl'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle
      - uses: Pyrenyl/build-support@main
        with:
          keystore: ${{ secrets.RELEASE_KEYSTORE }}
          store-password: ${{ secrets.RELEASE_STORE_PASSWORD }}
          key-alias: key0
          key-password: ${{ secrets.RELEASE_KEY_PASSWORD }}
      - run: ./gradlew assembleRelease --no-daemon
      - uses: actions/upload-artifact@v7
        with:
          name: Anywhere-release
          path: app/build/outputs/apk/release/Anywhere-2.5.5-2050500-release.apk
          if-no-files-found: error
